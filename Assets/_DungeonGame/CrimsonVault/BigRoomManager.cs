using UnityEngine;

namespace ZiercCode._DungeonGame.CrimsonVault
{
    public class BigRoomManager : MonoBehaviour
    {
        // Start is called before the first frame update
        private void Start()
        {
        }

        // Update is called once per frame
        private void Update()
        {
        }
    }
}